# Copilot instructions (adapter)

Canonical repo instructions: `AGENTS.md` at the repository root.
Read and follow it before broad exploration; use its file map, then
`docs/agent/ARCHITECTURE.md` only if deeper context is needed.

Critical: NEVER merge a PR and NEVER enable auto-merge — a human merges.
